<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!--
  ~ Copyright(c) 2015 BirdLetter. All rights reserved.
  ~ This software is the proprietary information of BirdLetter.
  -->

<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
    <comment>properties</comment>
    <entry key="admin.db.url">${admin.db.url}</entry>
    <entry key="admin.db.username">${admin.db.username}</entry>
    <entry key="admin.db.password">${admin.db.password}</entry>

    <entry key="log.db.url">${log.db.url}</entry>
    <entry key="log.db.username">${log.db.username}</entry>
    <entry key="log.db.password">${log.db.password}</entry>

    <entry key="logger.root">${logger.root}</entry>
    <entry key="logger.level">${logger.level}</entry>
    <entry key="logger.name">${logger.name}</entry>
    
    <entry key="service.isQuartzUseCheck">${service.isQuartzUseCheck}</entry>
    <entry key="file.uploads.path">${file.uploads.path}</entry>
    <entry key="file.view.url">${file.view.url}</entry>
    
    <entry key="google.email.username">${google.email.username}</entry>
    <entry key="google.email.password">${google.email.password}</entry>
    
    <entry key="service.isUseEmail">${service.isUseEmail}</entry>
    
    <entry key="service.version">${service.version}</entry>

    <entry key="school.search.api.key">${school.search.api.key}</entry>
    <entry key="school.search.api.url">${school.search.api.url}</entry>

    <entry key="flowedu.api.url">${flowedu.api.url}</entry>

    <entry key="session.use.yn">${session.use.yn}</entry>

    <entry key="academy.thumbnail.url">${academy.thumbnail.url}</entry>
</properties>